spring:
  application:
    name: gerenciamento-de-apiario
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/gerenciamento_de_apiario
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret: secret

server:
  port: 8080
